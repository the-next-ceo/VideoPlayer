name: vid_player
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

# video-player dependency
  video_player: ^2.2.19

  chewie: ^1.3.0
#cupertino-dependency
  cupertino_icons: ^1.0.2

#file-picker dependency (pick the file form the local storage)
  file_picker: ^4.3.3

# flutter alternative of android toast (to display messages)
  fluttertoast: ^8.0.9

  flutter_colorpicker: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  




